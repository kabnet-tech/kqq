---
name: Bug report
about: Report something kq does wrong — wrong output, crash, or error
title: ''
labels: bug
assignees: ''
---

**kq version** (output of `kq --version`):

**OS / platform**:

**Command** (exact query string and flags):

```
kq '...'
```

**Input data** (minimal sample, 1–3 lines):

```json
{"paste": "here"}
```

**Expected output**:

```
```

**Actual output**:

```
```

**Additional context**
If the output is wrong rather than an error, what does `jq` produce for the
same data? (optional but helpful)